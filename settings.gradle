rootProject.name = 'password-hero'

include 'android'
project(':android').name = 'password-hero-android'

include 'core'
project(':core').name = 'password-hero-core'

include 'standalone'
project(':standalone').name = 'password-hero-standalone'

